// Placeholder for the read-side diagnostic sink -- the markdown-codec equivalent of pdf-codec's own three-tier PdfDiagnosticSink policy (throw for unreadable input, recover-with-diagnostic for malformed-but-salvageable markdown, degrade-with-diagnostic for an individual unsupported construct while the rest of the document still reads). Will define MarkdownDiagnosticSink and NOOP_DIAGNOSTIC_SINK once readMarkdown's own scan/block/inline pipeline exists to call it.
