// Placeholder for raw-HTML-in-markdown handling -- CommonMark's own block-HTML and inline-HTML constructs (an html_block/html_inline node carries its literal HTML text verbatim, matching CommonMark's own "markdown does not require balanced/valid HTML" rule). No HTML parser dependency: recognising the seven block-HTML start/end conditions and inline tag syntax is a bounded, hand-written scan, not general HTML parsing.
