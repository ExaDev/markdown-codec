// Placeholder for the AST -> ContentDocument lowering stage: maps this package's own markdown AST (src/ast/) onto document-schema.js's shared ContentDocument pivot (see src/read.ts's own top-of-file note on the full-ContentDocument-vs-bare-shape decision this stage's own return type follows). The markdown-side counterpart to ooxml.js's readDocx/readPptx and odf.js's readOdt/readOdp -- a thin adapter from a format-specific parse result onto the shared pivot, not a second parser.
