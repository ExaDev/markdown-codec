// writeMarkdown: ContentDocument -> markdown source text. The build-side counterpart to src/read.ts's readMarkdown -- see that file's own top-of-file comment for the full-ContentDocument-vs-bare-shape reconciliation decision both sides of this pair follow. Not yet implemented; will compose src/emit's rendering stage over a full ContentDocument value, mirroring ooxml.js's buildXlsxPackage(document: ContentDocument) signature rather than odf.js's bare-shape build*Package equivalents.
